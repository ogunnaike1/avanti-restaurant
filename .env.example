# Reservations are delivered over WhatsApp, not email: the booking form hands
# the guest's own WhatsApp client a pre-written message addressed to the house.
# That needs no credentials, so this file is currently empty.
#
# The house number lives in lib/whatsapp.ts (WHATSAPP_NUMBER). Change it there
# and the float button, footer, contact page and booking form all follow.
